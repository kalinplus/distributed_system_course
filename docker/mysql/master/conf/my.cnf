[mysqld]
# 服务器唯一ID，主库用1
server-id = 1

# 开启二进制日志（主从复制必须）
log-bin = mysql-bin

# 只复制这个数据库
binlog-do-db = ecommerce

# 二进制日志格式
binlog-format = ROW

# 字符集
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
